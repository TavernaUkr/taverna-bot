services:
  - type: web
    name: taverna-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_updated.py
    plan: free
    envVars:
      - key: PORT
        value: 10000
